telegram:
  archive-channel-id: ${TELEGRAM_ARCHIVE_ID:-1003811027138}
  bot:
    name: ${TELEGRAM_BOT_NAME:fpvreport_bot}
    token: ${BOT_TOKEN}
    admin-ids: ${TELEGRAM_BOT_ADMIN_IDS:7158449055,377451642}

spring:
  security:
    oauth2:
      client:
        registration:
          fpv-bot:
            provider: custom-provider
            client-id: ${SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_FPV_BOT_CLIENT_ID:fpv-client}
            client-secret: ${SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_FPV_BOT_CLIENT_SECRET:fpv-secret}
            authorization-grant-type: client_credentials
            scope:
              - openid
              - "fpvreport:write"
              - "fpvreport:read"
        provider:
          custom-provider:
            token-uri: http://auth-server.railway.internal:9000/oauth2/token