spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/fpv_resource
    username: fpv_user
    password: password
  flyway:
    enabled: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000